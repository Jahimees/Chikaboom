CREATE TABLE `chikaboom`.`user_gallery` (
                                            `iduser_gallery` INT NOT NULL AUTO_INCREMENT,
                                            `idaccount` INT NOT NULL,
                                            `file_path` VARCHAR(45) NOT NULL,
                                            PRIMARY KEY (`iduser_gallery`));
