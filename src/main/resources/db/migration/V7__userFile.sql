CREATE TABLE `chikaboom`.`user_file` (
                                            `iduser_file` INT NOT NULL AUTO_INCREMENT,
                                            `idaccount` INT NOT NULL,
                                            `file_path` VARCHAR(100) NOT NULL,
                                            PRIMARY KEY (`iduser_file`));
